// folloowed from tutorial - didn't work as fields must be initialised;

class Book {
  String title;
  String author;
  int numPages;

  Book(String title, String author, int numPages) {
    this.title = title;
    this.author = author;
    this.numPages = numPages;
  }
}

void main() {
  print('yoyoyo');
}
